# PRAVDA

PRAVDA — React-прототип доказательной навигационной платформы для живых политических разговоров.

Четыре главных представления:

- **Хроно** — интерактивная временная шкала документов и событий;
- **Связи** — mind map / knowledge graph: что ведёт к выбранному узлу и что из него следует;
- **Аргументы** — проверяемая реконструкция тезисов Александра с посылками, возражениями, оговорками, `VIDEO_ID` и таймкодами;
- **AI** — frontend-прототип помощника: транскрипт, обнаружение тем, карта разговора и варианты ответа.

## Что уже работает

### Хроно

- перетаскивание шкалы мышью и пальцем;
- инерционное движение;
- `Cmd/Ctrl + колесо` для масштабирования вокруг курсора;
- даты над осью, события под осью;
- автоматические дорожки для близких событий;
- отдельные справочные страницы событий;
- RU / EN / UA и светлая / тёмная темы.

### Связи

- граф на `@xyflow/react`;
- выбранный узел всегда находится в центре;
- входящие связи расположены слева, исходящие — справа;
- режимы глубины в один и два шага;
- разные типы связи: прямая, документированная и интерпретационная;
- поиск по узлам;
- инспектор узла с пояснениями, источниками и переходом к статье события.

Корпус содержит события, документы, концепты и аргументы, соединённые прямыми, документированными и интерпретационными связями. Он предназначен как проверяемая развивающаяся модель, а не как законченная историческая база.

### Аргументы

- 14 содержательных аргументов из первых десяти канонических транскриптов канала «Взгляд из Четвертого Измерения»;
- отдельные поля для тезиса, посылок, хода рассуждения, вывода, возражения и ответа;
- уровень уверенности интерпретации и редакционные ограничения;
- точные фрагменты канонического транскрипта, ссылки на YouTube с таймкодом и первичные документы для внешней проверки;
- переходы к связанным событиям, другим аргументам и узлам графа.

### AI

- обязательное подтверждение уведомления участников разговора;
- запуск и остановка браузерного распознавания речи, если доступен Web Speech API;
- ручное редактирование или вставка транскрипта;
- демонстрационный разговор без микрофона;
- локальное обнаружение тем по ключевым словам;
- динамическая карта темы и связанных документов;
- три ответа: уточняющий вопрос, документальная формулировка и граница допустимого вывода;
- копирование ответа;
- переход к связанным событиям и карте.
- переход к аргументам, извлечённым из канонических транскриптов.

Текущий AI — **не production-ИИ**. Он не вызывает LLM и не отправляет аудио на backend. Полная потоковая архитектура описана в [`docs/PLAN.md`](docs/PLAN.md) и [`docs/AI_REALTIME_PROTOCOL.md`](docs/AI_REALTIME_PROTOCOL.md).

## Стек

- React 19 + TypeScript;
- Vite;
- React Router с `HashRouter`, чтобы статическая публикация работала без серверного rewrite;
- React Flow (`@xyflow/react`) для графа;
- Zustand с persist middleware для темы, языка и выбранного узла;
- Vitest и ESLint; включены unit-тесты распознавания тем, графового layout и целостности событий, аргументов, транскриптов и связей.

## Запуск

Требуется Node.js 22.12+ (либо совместимая ветка Node 20.19+).

```bash
npm install
npm run dev
```

Production-сборка:

```bash
npm run build
npm run preview
```

Проверка ссылочной целостности данных:

```bash
npm run verify:data
```

## Структура

```text
.
├── .github/workflows/pages.yml
├── docs/
│   ├── AI_REALTIME_PROTOCOL.md
│   ├── CODEX_HANDOFF.md
│   ├── CONTENT_MODEL.md
│   ├── PLAN.md
│   └── TRANSCRIPTION_PIPELINE.md
├── public/
├── scripts/
│   ├── publish-github.sh
│   └── verify-data.mjs
├── src/
│   ├── components/
│   ├── data/
│   ├── domain/
│   ├── hooks/
│   ├── store/
│   ├── utils/
│   ├── views/
│   ├── App.tsx
│   ├── main.tsx
│   └── styles.css
├── .env.example
├── CHANGELOG.md
├── index.html
├── package.json
└── vite.config.ts
```

## GitHub Pages

Workflow уже находится в `.github/workflows/pages.yml`.

1. Создайте репозиторий и отправьте ветку `main`.
2. В GitHub откройте `Settings → Pages`.
3. В `Source` выберите **GitHub Actions**.
4. Следующий push соберёт и опубликует `dist`.

### Стандартное правило деплоя для этого проекта

В этом репозитории действует правило: после любого изменения кода или контента изменения нужно сразу **фиксировать и пушить** в `main`.

Порядок:

1. Внести изменения.
2. `git add <files>`
3. `git commit -m "..."`
4. `git push`
5. GitHub Actions автоматически:
   - выполняет проверку данных,
   - тесты,
   - lint,
   - сборку,
   - публикацию в GitHub Pages.

Т.е. не нужно ждать отдельного запроса “сделай деплой” — по умолчанию каждый push в `main` идёт в deploy-пайплайн.

### Как транскрибировать видео канала Александра

Актуальный конвейер полностью локальный: оригинальное YouTube-аудио, полный
`faster-whisper large-v3` с фиксированным русским языком и локальная идентификация
голоса Александра через WeSpeaker ECAPA. `youtube-transcript.ai` больше не является
основным источником из-за дублирования реплик и низкого качества текста.

Полные команды, структура хранения, текущая точка продолжения, правила голосового
банка и порядок ручной докалибровки находятся в
[`docs/TRANSCRIPTION_PIPELINE.md`](docs/TRANSCRIPTION_PIPELINE.md).

Канонические результаты хранятся как
`transcripts/vzglyad-iz-chetvertogo-izmereniya/<VIDEO_ID>.md`. Техническое аудио,
JSONL и голосовые эмбеддинги остаются только в `.local/`.

Для создания репозитория через GitHub CLI:

```bash
./scripts/publish-github.sh pravda private
```

## Ограничения браузерного аудио

Обычная веб-страница не может скрытно слушать системный звук или вкладку чат-рулетки. Текущий прототип работает только с разрешённым микрофоном и поддерживаемым браузером. Для полноценного диалога нужны:

- явное согласие участников;
- `getDisplayMedia` для добровольного захвата аудио вкладки либо расширение браузера;
- потоковый STT на backend;
- диаризация или другой способ различать собеседников;
- политика хранения и удаления записей.

## Принципы качества

PRAVDA должна отделять:

1. текст первичного документа;
2. установленный факт;
3. историческую причинную гипотезу;
4. политическую или моральную оценку;
5. риторический приём.

Интерпретационные связи в графе визуально отличаются от прямых. Ответы AI должны иметь источники, показывать сильное возражение и не выдавать спорную причинность за доказанный факт.

## Документация для продолжения

Главный файл передачи проекта — [`docs/CODEX_HANDOFF.md`](docs/CODEX_HANDOFF.md).
Регламент корпуса видео —
[`docs/TRANSCRIPTION_PIPELINE.md`](docs/TRANSCRIPTION_PIPELINE.md). Подробная
продуктовая и техническая дорожная карта — [`docs/PLAN.md`](docs/PLAN.md).

## Лицензия

Лицензия пока не выбрана. До публичного распространения исходного кода необходимо отдельно решить, будет проект закрытым, source-available или open-source.
